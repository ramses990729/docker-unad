# Comandos básicos para el uso de docker

docker compose up -d        # levantar (la flag -d sirve para que no imprima constantemente los logs)
docker compose down         # bajar
docker compose logs         # ver qué pasa si algo falla
docker compose down -v      # reset total incluyendo datos
docker ps                   # ver qué está corriendo